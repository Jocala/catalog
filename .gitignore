/target/
catalog-swift/build/
.DS_Store

# Windows (WPF) — CatalogWin inside windows/
windows/**/bin/
windows/**/obj/
windows/**/.vs/
windows/**/TestResults/
# Inno installer output (built on win10, staged to debian, never committed)
windows/install/
# Recipe-owned native DLL copies (win10: copied into the project dir before
# build so publish bundles/ships them; rebuilt from crates/ each time)
windows/src/CatalogWin/catalog_ffi.dll
windows/winui/CatalogWinUI/catalog_ffi.dll
build